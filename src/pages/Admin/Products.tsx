import React from 'react';
import { Text, View } from 'react-native';

const Products = () => {
    return (
        <View>
            <Text>
                Tela de produtos (tela temporária)
            </Text>
        </View>
    );
}

export default Products;